{
  "name": "canvas-assignment-extension",
  "version": "1.3.0",
  "description": "View all Canvas assignments across courses",
  "scripts": {
    "build-css": "tailwindcss -i ./input.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
